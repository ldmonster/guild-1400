# Progress Index

Per-area reconstruction status (rule 12). Each entry links a focused progress file;
keep them current as work lands. The legacy monolithic log is `../PROGRESS.md`.

| Area | File | Status |
|---|---|---|
| Command recon4 — target-resolution resolvers + entity-action senders | [command-recon4-resolve-senders.md](command-recon4-resolve-senders.md) | `src/sim/command_recon4_{resolve,senders}.*` (namespace `guild::sim`). 1:1: the 10 `VIBE_Command_ResolveTarget*` console/history resolvers (Clergy 0x4f9238, PersonByName 0x4f9518, PersonAlt 0x4f989c, PersonScoped 0x4f9c20, BestRated 0x4f9f74, CraftWorker 0x4fa50c, ByStatGroup 0x4fa818, ByProfessionRange 0x4fac80, WoundedPerson 0x4faab8, RandomCarried 0x4faf54) — parse prologue, kind dispatch, 768-slot scan with exact profession-range/wealth/top-5-bubble-sort/random-stride logic, back-write to `params+8*idx+4`; selection table + leaves via inert `Recon4ResolveHooks`. Senders (control flow + packet field layout 1:1, queue/UI leaves via recording `Recon4SenderHooks`): `CommandRetZero` 0x493a08 (renamed, ODR), SendEntityActionA 0x5675ac, B 0x567944, C 0x567e24, D 0x56801c, SendMapEntityAction 0x568278. **Wave-2 (2026-06-11): the three formerly-omitted senders reconstructed 1:1 from dedicated disasm passes** — QueueRequestTransform64 0x4952b4 (opcode-0x40/47-byte packet, 0x80-byte rec copy, u32-wraparound + bit-exact binary32 deltas vs the FindSlotByProt 0x5851fc slot), EnqueueLawAction 0x4c2218 (+ the 51×40-byte law table 0x4c1810 verbatim; Cmd15+Args25 then type-0 stat delta / type-1 768-slot Coord27 scan), EnqueueDuelChallenge 0x538224 (276-byte packet via QueueRequest39 0x494c30; the original's IterNext stale-ECX infinite-loop defect preserved + test-guarded). Wired via `InstallRealRecon45Wiring` (live CommandQueue + g_persons). Named gap: VIBE_Combat_SetUnitFormationMode 0x48980c (garbled decompile, needs own disasm pass) — inert hook. +15 tests / 531 checks (`command_recon4_senders2_test`). Resolver dispatch table 0x6343d8 wired via history-parse (see history-parse-context.md). |
| Input poll orchestration + entity selection / selection-reset | [input-select-recon.md](input-select-recon.md) | `src/play/input_recon_select.*`. 1:1: `VIBE_Input_PollMouseAndKeyboard @0x40da88` (poll orchestrator, device steps hooked), `VIBE_SelectEntity_ComputeResult @0x4147cc` (48-actor selection-volume hit-test over the child-hotspot rect tables; full LABEL_6/29/27 goto graph incl. goto-into-loop; entity arrays/anim/projection hooked), `VIBE_Selection_Reset @0x4b9444` (anchor clear + owner resolve + child flag-bit-2 clear on type-29; query iterators hooked), `VIBE_Coord_ConvertX @0x5c6b08` (frndint round-to-nearest). SKIPPED (rule 4, platform boundary, routed via hooks): BuildScancodeTable 0x40c710 / FormatErrorMessage 0x40c950 / Acquire{Mouse,Keyboard}Device 0x40c9b8/0x40c9f8 / ReadMouseAxes 0x40d7c8 (DirectInput/Win32). 64-bit fidelity: hotspot-list (count,idArray) + owner-B record decoded at the hook boundary (packed 32-bit-pointer overlap). 12 tests / 40 checks (ASan-clean). Wiring PENDING on unreconstructed callers (GameTick_MainLoop 0x414a38, Hud/Scene/Building enters). |
| Main menu + New Game / ChooseCity 3D scene | [menu-choosecity.md](menu-choosecity.md) | ChooseCity 1:1 — engine projection + exact MegaCam-object camera (eye=dummy+92, rot=MatrixFromEuler(−(dummy+144))) + single moving sp_STADTTURM tower pick (VIBE_Menu_RunChooseCity) + baked 7-band lighting + camera-flight spline + backface-cull, wired into native New Game |
| 3D scene GPU pipeline (Vulkan shaders) + FPS | [render-gpu-pipeline.md](render-gpu-pipeline.md) | Backend-neutral draw list (engine T&L) → CPU reference raster (oracle) OR Vulkan GLSL pipeline (glslc at build time); GPU==CPU validated on lavapipe; per-frame draw-list + GPU-upload caching (camera-only frames skip uploads) |
| New-Game flow after city choice | [menu-newgame-flow.md](menu-newgame-flow.md) | `VIBE_Menu_ChooseCharacterIntroVariant @0x52e4e0` = the DIFFICULTY picker (markup `_M0_DIFFICULTY`): reconstructed 1:1 (radio-of-6 → `byte_12335BA`) + golden tests, AND a native playable SDL screen wired after `RunCityScreen3D`. `RunChooseHistory @0x52d684` = the PERSPECTIVE picker (`_M0_HISTORIE`, 3 modes → History flag 1/2/0): reconstructed 1:1 + native screen, wired after the difficulty screen (flag → `NewGameParams.historyFlag`). `ChooseCharacterIntro @0x52e3d8` (the spine router: manual family-tree → dynasty / automatic → profession / abort) reconstructed 1:1 + golden tests. `RunChoosePlayer @0x52ccd8` identity wizard (Vorname→Nachname→gender→faith→wappen→commit) reconstructed 1:1 AND built as a native playable SDL wizard (real labels + live text entry via a new SDL-text-input `pollText()` platform capability + wappen sprites), wired after the perspective screen. `RunChooseCharacter @0x52bcd4` (3D dynasty/ancestry) run loop reconstructed 1:1 + golden tests (drives gui/charcreate's slot-fill logic; native scene awaits the character-animation subsystem). New-game screen logic now reconstructed end-to-end. `VIBE_Anim_CalculateAnimNormals @0x5d0020` (per-frame deformed-mesh vertex normals + neighbour-smoothed bbox) reconstructed 1:1 + golden/real-`.baf` tests — closes the "normals stay static" gap in the (already ~95% reconstructed) character-animation subsystem. `render/anim_relight.{h,cpp}` (`RelightPosedFrame`) is the glue feeding `CalculateAnimNormals` (0x5d0020) output into `VIBE_Mesh_ComputeVertexLighting` (0x5c9054): it writes a frame's per-vertex normals into each `VertexSource.normal` (+12) then runs the env-map lighting walk, so posed/deformed character frames are lit with CURRENT-frame normals instead of stale rest-pose ones (golden `anim_relight_test`, 16 checks) |
| Skeletal/object pose driver | [anim-skeleton-pose-driver.md](anim-skeleton-pose-driver.md) | `VIBE_Anim_UpdateSkeletonPose @0x5cd1d8` (the 6.6 KB per-frame pose DRIVER) reconstructed 1:1 as a control-flow orchestrator in `render/skeleton_pose_driver.{h,cpp}`: time-split/early-out, the LOD-layer + 3-per-bone-track advance ladders (loop/ping-pong/clamp/hold + repeat counter + settle break), blend-window weight, attachment window, palette rebuild, the object morph Catmull-Rom advance + terminal/free, and the vegetation light-cache gate. Calls `AdvanceFrameIndex` (0x5ccf18) directly; the genuine scene-graph/asset side effects + the per-axis morph blend route through an inert-default `SkeletonPoseHooks`. Golden `skeleton_pose_driver_test` (10 vectors, 39 checks). Named gaps (rule 8): per-keyframe phase rate, attachment lerp/rotate push, morph per-axis Catmull-Rom — all hooks, none faked |
| Character-from-model factory trio | [character-factory.md](character-factory.md) | `VIBE_Character_CreateFromModel @0x402d10` / `CreateMesh @0x4029c4` / `AllocSlot @0x402254` (reused) reconstructed 1:1 in `sim/character_factory.{h,cpp}` — the single creation path for all dynasty actors AND live NPCs. Exact name decomposition (base→+304 / prefix→+368, recovered from the StrChr disasm), the record/object-node field+flag writes, the `strstr` creature-type probe (RATTE/HUND/KATZE/PFERD), and the gait+idle preload. Scene-graph/model-load/anim leaves + `Destroy @0x402120` routed through inert-default `CharacterFactoryHooks`. Golden `character_factory_test` (12 tests, 66 checks) |
| Mesh LOD filename strategy + stock-object load-or-find + LOD attach | [mesh-lod-stock-object.md](mesh-lod-stock-object.md) | `VIBE_Mesh_BuildLodFileName @0x5d15fc` (PURE LOD-name strategy: base / "_s" / "_<lod>" per the `byte_64A098` LOD mode incl. the mode-2 downward `"%s_%i"` existence probe + the explicit-LOD index flip), `VIBE_Mesh_BuildTexturePath @0x5d1034` (`"*"+name+suffix`, existence via hook), `VIBE_Mesh_AttachStockObjectLods @0x5d1824` (faithful base@244 / "_s"@1396 / LOD-frame@244+384*lod attach structure, draw-block writes via inert-default `MeshLodHooks`), and `VIBE_Mesh_LoadOrFindByName @0x5d345c` (orchestrates BuildLodFileName + the EXISTING `MeshAssetCache::LoadOrFind` — base → "_s" → LOD 1/2). New `src/render/mesh_lod_name.{h,cpp}`. Golden tests: `MeshLodName` 35 checks, `MeshAttachLods` 23 checks, `MeshLoadOrFind` 20 checks. Gap (rule 8): `AttachStockTextures @0x5d1114` body = scene-graph leaf behind the hook. |
| Universe render chain (model-load → per-frame render) | [universe-render-chain.md](universe-render-chain.md) | **LIVE LOOP STOOD UP + TEXTURED + ON-SCREEN (2026-06-10):** `play::UniverseFrameDriver` (`src/play/universe_render.*`) drives a REAL `Objects.BIN` mesh through the engine frame spine `RenderMainViewFrame 0x5B6074 → BeginUniverseFrame 0x5B3900` (clear + `ProjectVerticesToScreen` + the REAL `ProcessSceneNodeAppend 0x5ADD1C` via `WalkSceneTree`/`InstallRealSceneBridge` + `RadixSortDrawList`) → `RasterizeMeshList 0x5AEC88`. TEXTURED via the real `RasterizeTexturedTriangleRgbz 0x5F6C30` (per-material `render::Texture` from `Textures.BIN` + 565 palette; `Polygon.matIndex` carried from `BgfPolygon`), and PRESENTED via `PresentToDevice` (rule 3/4: headless `MemoryGraphicsDevice`, on-screen `VulkanGraphicsDevice`+SDL). `universe_render_e2e` verified live: `Buch.bgf` 368v/604p → 216 tris, 5 materials, 58 textured polys, 191 colours, presented (19158 bb px). `SelectLodFrame 0x5adb6c` reconstructed+wired. The full chain reconstructed 1:1 + verified LIVE on a real `.bgf` (`object_attach_chain_e2e`, member BUCH 316v/604p): CreateMesh → `AttachToUniverseNode @0x5b3e30` → `Mesh_LoadOrFindByName @0x5d345c` (BuildLodFileName + stock cache `LoadAndRegister`/`FindStockObject`) → `AttachStockObjectLods @0x5d1824` → `AttachStockTextures @0x5d1114` → `RenderUniverseFrame @0x5b3de8` (`BeginUniverseFrame @0x5b3900` + `DrawUniverseAndStats @0x5b3bbc`: terrain, `ShadowResetLightList @0x5f4428`+`PushToDrawList @0x5f43f4` shadow-light collector [4 max, `shadow_light_list_test` 22 checks], `SceneGraph_WalkAndInvoke` → `ProcessSceneNode` draw-list → `RadixSort` → raster, per-object `UpdateSkeletonPose`). `DrawTexturedTriangles @0x5ae434` swapped for Vulkan/CPU (rule 3). Remaining hooked leaves need the live Vulkan/universe runtime (rule 8) |
| Game clock / tick / calendar (VIBE_Time / GameTime / GameTick) | [gametime-gametick-recon.md](gametime-gametick-recon.md) | GameTime record (de)serialize (`InitDefault 0x58320c`, `UnpackFromRecord 0x58334c` with the Europa-**1400** year bias), the reg-shuffle `AdvanceThunk 0x583374` → existing `GameTimeAdvance`, `GetScaledDelay 0x43c680` (script "GetTime"); the two entity hit-test scans `InitEntityTracking 0x4146d8` + `MainLoop 0x414a38` (512-slot 16.16 box pick, child/sub-node pointer deref, SelectEntity fallback functor); CRT POSIX-TZ parsing `ParseDecimal/ParseTzName/ParseTzRule` (0x6070b0/0x6070dc/0x6071f4), `Set/ClearTzInitFlag` (0x606f2c/0x606f10), DST transition math `DstTransitionDayOfYear 0x60693c` + `IsAfterDstStart 0x606a34` (exact month tables + MakeTimeUtc round-trip). New `src/sim/gametime_recon.*`, `src/sim/gametick_entityscan_recon.*`, `src/crt/tzparse_recon.*`. 88 checks passing. Deferred (rule 8, named): BeginRound 0x52f66c / RunAdvanceGameDialog 0x4c0750 / RequestStartTurn 0x579530 (cross-module); GmtimeToTlsBuffer 0x606eec / LoadTimeZoneFromSystem 0x606f54 / GetLocalTime 0x142216c (CRT/TLS/Win32 platform boundary) |
| Building / Bauplatz deferred cluster | [buildingtype-bauplatz-recon.md](buildingtype-bauplatz-recon.md) | Four functions building.cpp had deferred reconstructed 1:1 in `src/sim/buildingtype_recon.*` (namespace `guild::sim`, reuses `ObjectRec`/`BuildingTypeDef`): `VIBE_Bauplatz_GetSize @0x577628` (case-insensitive plot-table scan, stride-96 `BauplatzSizeRec`), `VIBE_Bauplatz_MapOneToSupermap @0x5774b8` (exact 4-corner quad assembly lo→hiX→hiX/hiZ→hiZ + WorldToTile + raster fill 255), `VIBE_Building_AllocStorageRoom @0x588988` (storage kind-42/floor-2 + market kind-278/group-6/floor-6 security clamps incl. the single-branch asymmetry), `VIBE_Building_RegisterNames @0x504a54` (candidate de-dup + length filter + uniform RandomModulo pick). Byte-faithful `Util_StrCmp` (0x5d3f10) / `StrCmpNoCase` (0x5cb8f0). Render/supermap/scene-graph/io leaves behind inert-default hooks (rule 8, named). Size-3 ReturnCode thunks 0x589a32..0x589a44 SKIPPED (already folded into building_type.cpp). 19 tests / 87 checks. Wiring PENDING on unreconstructed callers 0x504ce0/0x496b90/0x586fb8/0x577464. |
| In-game perf overlay (Steam-style FPS) | [perf-overlay.md](perf-overlay.md) | Additive host diagnostic: smoothed FPS + frametime + frametime graph, configurable corner/detail, env (GUILD_PERF_OVERLAY) + F11 hotkey; wired into the 3D city pick + main menu present loops |
| Camera big movers (rotate/move/zoom/orient/track) | [camera-recon2-movers.md](camera-recon2-movers.md) | Second-pass camera cluster reconstructed 1:1 in `src/render/camera_recon2.*` (reuses first-pass `camera_recon` model + AnchorToTerrain): `RotateView @0x4b300c`, `UpdateMovement @0x4b41a8` (drag-latch → pan/wheel-zoom/rotate state machine), `ZoomReset @0x4b5250`, `ZoomOut @0x4b5974` (→3D listener kind 104), `OrientToTarget @0x4b562c` (→listener kind 40), `UpdateTrackTargetFromMouse @0x5e967c` (dual edge-tracked drags + axis-lock routing tree). Pure math/transform leaves reused (MatrixCopy/Fmod/VectorNormalize/VectorAngleWrapped/AcosGuarded/VectorWithinTolerance/PointThroughBoneChain); coupled leaves (terrain/mesh-height/3D-sound/family-record/scene-graph/light/constraints) behind inert-default `Camera2Hooks` (rule 8, named). Golden `camera_recon2_test` 13 tests / 34 checks. Caller wiring PENDING on unreconstructed parents (Camera_Update 0x4b4c68, RunMainFrameLoop 0x50f0c0, building loader 0x50d01c) |

| PLAYABLE SESSION WIRING (wave: "wire up to playable game") | this row + the per-slice files below | **2026-06-11: the native city session (`play::RunSdlSession`, `guild_run --play`) now wires the reconstructed subsystems together per frame**: REAL camera (`session-camera.md` — VIBE_Camera_Update @0x4b4c68 reconstructed + SessionCamera adapter; the old pan→render dead link closed: eye/zoom now feed Options.eyeX/eyeZ/pixelsPerUnit), HUD overlay (`session-hud.md` — SessionHud over the real 16bpp draw leaves; money/date/status/bar; the VIBE_Shape_ConvertRgbTo16 @0x5d7c0c gap was CLOSED in wave 1 — see shape-convert16.md), real selection (`session-select.md` — Selection_CommitContact @0x4b950c reconstructed + Selection_Reset @0x4b9444 on empty/right click), persons rendered (`session-characters-render.md` — g_persons → ResolveStaffModel @0x57c1e8 → UpdateSkeletonPose @0x5cd1d8 posed character meshes, opt-in scanPersons, ON in the session), real audio (`session-audio.md` — SessionAudio: SoundSystem + MusicDirector + the 0x4c09a0 audio tick + market ambience over SDL; music_world reroll bug fixed 1:1), day/night+weather driver (`session-atmos.md` — SessionAtmos; finding: brightness applies via lighting-table rebuild pre-raster, no frame post-process; renderer consumption pending), quicksave/quickload F5/F9 (`session-save.md` — original partial .SAV format write reconstructed @0x5a348c, round-trip + byte-compare validated; menu Load-Game hookup pending), options persist real settings (`session-settings.md` — WritePrivateProfileStringA merge semantics 1:1 into Gilde.INI), scripted full-flow e2e (`playable-flow-e2e.md` — menu→new game→AUGSBURG→session→ESC→menu→quit, 35 checks). The two former PENDING items LANDED 2026-06-11: new-game identity plumbing/commit ([newgame-apply.md](newgame-apply.md) — NativeMenuResult carries full NewGameParams; ApplyNewGameParams @0x5336f0 + the InitOrLoadSession @0x533a54 new-SP block commit player/parents/relations/start-gold into the live world) and the continuous game-time tick ([session-tick.md](session-tick.md) — the 994ms clock proc @0x527778, opcode-30 master→world sync via ExAdvanceGameTick @0x498954, day-end at hour≥23 → RunGameDay → next-day 06:00). |
| WAVE-1 SLICES (2026-06-11, "make the game playable") | per-slice files | Scene main frame loop @0x50f0c0 ([scene-main-frame-loop.md](scene-main-frame-loop.md), 26 tests/142 checks); building scene attach @0x50d01c + ReadCityObjectRecord @0x5e67c8 — the .cty-embedded persisted scene stream (tag 0x3A6C00BB) discovered as the real placement source ([building-scene-attach.md](building-scene-attach.md), 19/170); whole-city real-3D CityView3D over the universe chain + io::LoadWorldEx ([city-view3d.md](city-view3d.md), 7/52 + e2e 4/52); shape converters @0x5d7c0c/@0x5d7924 + ShapeBankConvertNew @0x5d80a8 → real gilde.gfx HUD artwork ([shape-convert16.md](shape-convert16.md), 12/76 + e2e 2/43, the session-hud named gap CLOSED); game clock tick + SessionTick ([session-tick.md](session-tick.md), 12/66 + 14/69); history-parse context @0x4fd44c wiring ([history-parse-context.md](history-parse-context.md)); buildingtype callers @0x504ce0/@0x504a54/@0x586fb8/@0x496b90/@0x577464 ([buildingtype-bauplatz-recon.md](buildingtype-bauplatz-recon.md), 32/141 — the wave-1 PENDING callers now reconstructed + wired); Load-Game menu flow @0x56a270/@0x569d00 → NativeMenuResult::kLoadGame + savePath ([menu-loadgame.md](menu-loadgame.md), 10/244 + itest 3/26); atmos→lighting-table consumption @0x5c886c/@0x5c8218/@0x5c7f04/@0x5c7da4 ([atmos-lighting.md](atmos-lighting.md), 23/461); tooltips + info panels @0x4f7424/@0x4f7a10../@0x4b84c0 ([session-ui-panels.md](session-ui-panels.md), 18/207); opcode-27 relation handler fixed to live g_persons ([command-apply-relation.md](command-apply-relation.md)); camera EdgeScroll @0x4b2c34 + terrain AverageAreaHeight binding + CameraPose ([session-camera.md](session-camera.md), 14/75 + 17/71); real input/selection chain @0x40dab8/@0x40cdd0/@0x40d920/@0x4b950c ([session-input.md](session-input.md), 16/119). |
| WAVE-2 SESSION INTEGRATION (2026-06-11, "playable game" landed) | [session-integration-wave2.md](session-integration-wave2.md) | **`guild_run --play` now runs the integrated playable session**: menu → new-game commit (ApplyNewGameParams) → real-3D city (CityView3D through the universe chain, SessionCamera::pose(), terrain-bound) → continuous clock (SessionTick; day rollover runs RunGameDay) → SessionInput as the one pick/selection driver → SessionHud with real converted gilde.gfx banks + tooltips/info panel → atmos lighting (universe ambient reads live LightAtmos globals) → Load-Game path (kLoadGame → LoadLiveWorld, no new-game commit). All SdlSessionConfig/Trace additions are additive (wave-1 byte-identical defaults). Two pre-existing ASAN bugs fixed: Form_InitTables 512-into-511 g_widgets OOB write; unclipped RGBZ textured raster spans @0x5F6C30 (surface-clip clamp, pixel-neutral in-bounds, golden-pinned). playable_flow e2e now 5 tests/76 checks (player identity in g_persons, clock fired, 796 instances rendered, hash+pixel determinism); raster_clip_test 3/9; verified on the vulkan-sdl-system backend (llvmpipe headless). Deferrals documented in the file (SceneMainLoop adoption, terrain height fill, persons in 3D view, panel record feeds, mouse wheel axis, rateByte scalar). |
| WAVE-2 FIDELITY FIXUPS (2026-06-11) | [fixups-wave2.md](fixups-wave2.md) | Four diagnosed divergences closed + one wiring gap: SceneClassifyMeisterRecords (0x504ce0 kernel) now overwrite-until-both-found (last match wins) like the binary; relation grid A stride corrected to 768 bytes/row with `RelationState::matrixA` aliased onto `world::g_relationMatrix` (one global, like the binary; static_asserts pin geometry); newgame_apply.h stale opcode-27 gap comment refreshed (gap CLOSED); ExSellObjekt's unfaithful `g_lastTradeId = moved` overwrite removed (only the faithful LABEL_58 dest-stock-node latch remains); the 0x533a54 NewGameSyncScene sink now dispatches the real `sim::Scene_SyncMeisterBuildings()` (`app/session_init.cpp`). Tests extended across cutscene_misc5 (82 checks) / apply6 (113) / buildingtype_callers (141) / session_init (67). |

| WAVE-3 RENDER FIDELITY (2026-06-11) | [terrain-colorkey-wave3.md](terrain-colorkey-wave3.md) | Two session-frame problems root-caused + fixed. (1) The "magenta" was NOT colour-key: untextured-material polys fell back to the 8bpp shaded span (@0x5F7960) writing shade bytes into the 16bpp framebuffer (0xC8C8 words). Fixed: surface-format guard on the 8bpp span; untextured 16bpp polys route through the engine's 1x1 white default binding (BindActive @0x5db564 slot==0) LEVEL-shaded (768*level keys of @0x5c5120) via the textured leaf @0x5F6C30; pink 3740→0 px. Colour-key machinery reconstructed (FillSpanTexturedMasked per PatchSpanConstantsMasked @0x5f753f, keyed routing on tex flags&8 — the 0x5db234 arm); named gaps: the key-trigger flag source, 24-bit BMP materials, seasonal texture sets (@0x5b3f54). (2) FLAT TERRAIN root cause corrected: heights are STORED in the scene stream's floor block ("<scene>_height" u8 grid via VIBE_WorldIo_LoadFloorRegions @0x5e78a8, validated over all 10 shipped .ed3 + the embedded .cty blob) — new `render/scene_floor` parses it and builds the Heightmap exactly as BuildTerrainMesh @0x5c5610; the session camera now follows real ground (AUGSBURG worldY 45.7..753.4). render_scene_floor_test 4/30, render_fidelity_w3a_e2e 4/4176. |
| WAVE-3 PERSONS IN THE 3D VIEW (2026-06-11) | [persons3d-wave3.md](persons3d-wave3.md) | Live `sim::g_persons` rendered as real posed character meshes in the same universe pass as the buildings: placement = VIBE_Character_SpawnAtBuildingEntrance @0x57c8f0 (anchor building's "dummy_EINGANG"/"dummy_TUER" via Object_FindByHandle @0x5b7be4 + PointThroughBoneChain @0x5c8b38; anchor from the person's +0x16C home / +0x170 work columns); model via ResolveStaffModel @0x57c1e8 → the REAL factory CreateFromModel @0x402d10 → Mesh_LoadOrFindByName @0x5d345c; posed via UpdateSkeletonPose @0x5cd1d8. Unanchored persons are NOT placed (counted — never an invented position). New `play/session_persons3d` entry points wired into the session (Wire after BindWorldObjects, Update per frame, Rebind after quickload). Named gaps: spawn-caller anchor sources (@0x4e7e88), walking tile→world (needs TileToWorld @0x5c65d4 binding), clip selection (NpcAction runtime), head variants, zero spawn rotation. Unit 5/50, e2e 2/69. |
| WAVE-3 SESSION UI FEEDS + LOOSE ENDS (2026-06-11) | [session-ui-feeds-wave3.md](session-ui-feeds-wave3.md) | Tooltip/info-panel record feeds now REAL: the session runs the original's session-start data load (World_LoadBuildingAndObjectData @0x5835f8 + City_InitParameterTable @0x577a9c) so the A_Geb/A_Obj catalogs are live; building hover resolves the live g_objects record (sale price via Building_ComputeSalePrice @0x591480), person hover the live g_persons fields (cash via PersonGetCashAmount @0x58bc9c); selection rides the building handle dword_631748 (wave-2 always built the STANDARD panel — fixed). Mouse wheel added to shim::MouseState (SDL_MOUSEWHEEL + scriptable), feeding the real wheel-zoom (@0x4b41a8). Load-game 3D derives the city from the save header (+0x05) else INI [General]/Stadt and loads stadt_<city>.ed3 (@0x500218); the engine save's scene-sidecar WRITE (@0x5e65b8) stays a named gap. SceneMainLoop adoption re-assessed: still blocked (runFrameLoop owns input/audio/clock internally); adoption plan written. playable_flow_e2e now 8 tests/119 checks; shim_wheel 4/13. |
| WAVE-3 ORCHESTRATOR STITCHES (2026-06-11) | this row | Cross-agent handoffs applied in sdl_session.cpp / city_view3d.cpp: (a) the session terrain bind now parses the embedded scene blob's floor heights (ParseSceneFloorHeights + BuildCityHeightmapFromFloor; flat-grid fallback when the loaded scene carries no floor block); (b) CV3D_SpanTextured mirrors universe_render's wired routing (keyed→masked leaf, fallback→SpanFillTexturedOpaque white default); (c) session_persons3d wired (boot/frame/quickload-rebind; trace personsRendered = Result::personInstances). guild_run headless --play parks the idle NullPlatform mouse at the screen centre ((0,0) is a held corner-scroll to the faithful EdgeScroll @0x4b2c34). Full suite after merge + stitches: **1415/1415**. |

| WAVE-4 LIVING CITY (2026-06-11) | [living-city-wave4.md](living-city-wave4.md) | Persons MOVE: the real path-follow (PathBuildWaypointList @0x43bd70 / WalkStep @0x4093b0) stepped once per opcode-30 world-clock commit; day-start destinations from the REAL daily director NpcDaily_DailyRoutineStep @0x4e7e88 (state-0 morning sweep via InstallRealNpcActions); live draw positions via record tile +0x6C/+0x70 → TileToWorld @0x5c65d4 over the real heightmap, entrance-dummy seats as static fallback; per-frame position update without rebind (CityView3D::MoveBoundPerson). Live .cty data carries no populated building columns (named gap) — the engine path dispatches nobody on untouched data, exactly as evidenced; e2e stages columns via the documented postWorldLoadHook seam. New src/play/session_npc_daily.{h,cpp}. Unit 4/70; e2e LivingCityPersonsMove 154 checks. |
| WAVE-4 MATERIALS: SEASONS + PALETTIZER (2026-06-11) | [materials-wave4.md](materials-wave4.md) | SEASONAL TEXTURE SETS fully reconstructed: season = day%4 (GameTime_GetSeasonFromDay @0x58339c; 0=F spring..3=W winter per the @0x505df4 dispatch), applied via the @0x506df4→@0x506388 foliage gate (pfl_/vg_/!vg_)→SelectTextureSet @0x5b3f54; the data source is the .TXS sidecar per .bgf (magic 0x23F209AE; row m = replacement material name; validated over all 590 shipped sidecars) — new render/texture_set_table wired into real_texture_source (foliage renders as textured trees; AUGSBURG unresolvable materials 34→14, the rest genuinely absent). 24-BIT BMPs: the REAL soft-palettizer chain reconstructed (VIBE_Texture_LoadSoftPalettize @0x5da34c → the @0x602d2c..@0x602c8c quantizer: 15-bit histogram octree, heap reduce, rounded-average palette, serpentine Floyd–Steinberg dither) in render/texture_palettize — 24-bit sources now palettize at the resolve layer like the engine (samples bucket to (c&0xF8)+4); the RGB-direct affine route remains an explicit default-OFF stand-in toggle. |
| WAVE-4 BINARY VERIFICATION SWEEP (2026-06-11, IDA MCP back) | [raster-verify-wave4.md](raster-verify-wave4.md) + [terrain-ground-wave4.md](terrain-ground-wave4.md) + [command-recon4-resolve-senders.md](command-recon4-resolve-senders.md) | Everything built while IDA was offline re-checked against the decompile. CONFIRMED: masked-span pair structure, RGBZ pipeline + clip clamp, HiColTab ramp, BuildTerrainMesh originY/scaleY (exact flt_628BA8 = 0x3B81848E), spawn-at-entrance placement, duel-loop defect. DIVERGES→FIXED 1:1: the floor-block grammar (scene_floor reconciled to the real @0x5e78a8 stream: elemSize+count array framing; heights/water/texture/lightOffset arrays + 344-byte water records — the "three size dwords" was a misread), the 8bpp span's late-carry adc chain + reverse vertex-load winding + the a4&5 tile-type stamp path (@0x5F7960/@0x5F7D58), RGBZ UV scale (×65536, not ×mipWidth) + the flags38 winding arm, masked-span light row pal[(avg+66)<<8|texel], meshlist level shade = AVG not max, npc-daily dispatch bit 0x1000 (not 0x100000), flt_628AE0 = −64.0. REFUTED: "flags&8 = colour-key trigger" (@0x5db234 only feeds the un-captured 0x5dea50) — bind sites now route keyed=false until 0x5dea50 is decompiled; "white default" is a host stand-in (the slot-0 binding is null), re-documented per rule 8. VIBE_Combat_SetUnitFormationMode @0x48980c recovered (eax=276-byte packet, dl=mode) and wired into EnqueueDuelChallenge — the last wave-2 sender gap closed. UNVERIFIED queue for IDA's return recorded in raster-verify-wave4.md. |
| WAVE-4 TERRAIN GROUND PASS (2026-06-11) | [terrain-ground-wave4.md](terrain-ground-wave4.md) | The 3D city draws its REAL ground: the @0x5bf22c RenderTerrain walk (LOD ladder @0x5bdec4→@0x5bef08→@0x5ba438 + edge stitch, real clip @0x5ad614, flush @0x5AEC88) over the reconciled floor data, hooked at the VERIFIED BeginUniverseFrame @0x5B3900 0x5b3a2f arm (after clear, before the object walk; DrawUniverseAndStats has NO terrain) under the same camera/projection as the buildings. New: the @0x5c4718 illumination-table build (15-pattern table @0x5c4690) + complete BuildLitTileGeometry @0x5c47dc (all three fill modes), the deterministic UV-table half of ComputeFilterWeights @0x5b94cc (render/terrain_uvtable), floor mask = N²−1 (evidence-backed fix). Ground meets building bases (909/926 instances within 10% of terrain span). Named gaps: tile textures (Floor_LoadTexture @0x5bd010 / GetOrBuildTile @0x5ba1e8 — level-shaded ground until decompiled), UV-table jitter tail, Pass-A row winding re-check. Session wired: opt3.terrain = view.hasGround() + camera bound to the view's @0x5c47dc-filled heightmap. terrain_ground_test 98; terrain_ground_e2e 34. Suite 1424/1424. |

| WAVE-5 BINARY RE-VERIFICATION (2026-06-14, IDA MCP live) | [colourkey-wave5.md](colourkey-wave5.md) + [rtx-verify-wave5.md](rtx-verify-wave5.md) + [ras-verify-wave5.md](ras-verify-wave5.md) | The wave-3/4 offline-built raster/texture reconstructions re-checked against fresh decompiles. CONFIRMED 1:1: the three edge interpolators (InterpolateEdgeRgbz @0x5f6930 / EdgeZ @0x5f6a8c / EdgeZTex @0x5f7840), the RGBZ leaf @0x5F6C30 pipeline + clip clamp, the soft-palettizer chain (@0x5da34c/@0x6029f0/@0x602d2c..), flt_62c3d4=65536.0f, the HiColTab ramp @0x5d9db8, the +72 per-texture light table (= shared HiColTab block, 63 ramp rows L=0..62 + 256 direct entries; lightRow8 range 0..62 — resolves the wave-4 "254 vs 64" question). DIVERGES→FIXED 1:1: the flat-fill winding (@0x603da8/@0x603d00 are span/slope only; winding lives in Shadow_RasterizeTriangle @0x603ED4 — cross-product + flag38 gate + reverse-load now reconstructed), the dispatch table (dword_13D8780 is SIX slots all → NullStub @0x5F6EE8 via InitEngineDevice @0x5AF984; the real textured path is D3D DrawTexturedTriangles @0x5AE434 — the apparent 7th slot is the clip vertex-pointer array; render_clip_test recalibrated). The terrain Pass-A winding (@0x5bf22c) re-decompiled: the wave-4 quad order was a guess and WRONG — the real diagonals (v494≥0: TL-BR, v494<0: BL-TR) reconstructed and the wave-4 row-mirror HACK removed (terrain now fed in true row order). |
| WAVE-5 COLOUR-KEY (the black-tree-backdrop fix) | [colourkey-integration-wave5.md](colourkey-integration-wave5.md) | Root-caused + fixed end-to-end. Texture record +104 **bit 2 (0x04)** is the colour-key flag (set for >8bpp/24-bit sources, gate dword_140809C=0 default ON); the wave-3/4 code wrongly used **bit 3 (0x08)** = the "_NM" mip-bias flag. The key VALUE is **black (0,0,0)** (VIBE_Bmp_LoadBuffer @0x5f0ce4's 24-bit arm never builds a palette → the LoadAndStretchTexture @0x5dea50 key pal[0]=0). Fix: producer sets kTexFlagColourKey for bpp>8; PalettizeDecodedBmp reserves index 0 for exact source-black on keyed textures; the masked span (skip index 0 @0x5F721A) then reproduces the DDraw source-black key; bind sites (universe_render/city_view3d) route `keyed = TextureIsColourKeyed(*bt->tex)`. Real AUGSBURG: black-backdrop pixels 1543→0, keyed strictly on exact (0,0,0) (no holes in dark-but-not-black art). The last wave-2 sender gap also closed: VIBE_Combat_SetUnitFormationMode @0x48980c recovered + wired into EnqueueDuelChallenge. |
| WAVE-5 TERRAIN TEXTURING (white ground → real textures) | [terrain-texturing-wave5.md](terrain-texturing-wave5.md) + [tile-textures-wave5.md](tile-textures-wave5.md) | The 3D ground now renders its REAL floor textures (WIESE/SAND/MOOS/FELS/stone/earth) instead of flat level-shaded white. W5-TILE reconstructed the per-cell resolver (Floor_LoadTexture @0x5bd010 cores + the slot-name→texture path via the real LoadByName) + FloodFillTileType @0x5c5530; W5-TX mounted a TextureAssetCache over Textures.BIN (with the 24-bit soft-palettize arm), bound the resolver to the 8 floor slot names, installed getTileTexture, and made GroundFrame rasterize each tile TEXTURED (RasterizeTexturedTriangleRgbz with the @0x5bf22c corner-inset UV table from BuildTerrainUvTable @0x5b94cc + the avg-light row) when a texture resolves, level-shaded fallback otherwise. Real AUGSBURG: 37827 ground pixels match the resolved slot texels (21918 WIESE). Recalibrated: real_texture_driver_e2e texturesDecoded 1949→2370 (24-bit BMPs now decode via the palettize arm). |
| WAVE-5 WATER REGIONS (build + animate; render pending) | [water-regions-wave5.md](water-regions-wave5.md) + [floorwater-wave5.md](floorwater-wave5.md) | VIBE_FloorWater_PrepareRegions @0x5ba95c (the water-mesh array builder) reconstructed 1:1 as render::BuildWaterRegions — water-mask dilation, height-gradient fill (FillHeightGradient @0x5ba898), region flood-fill (FloodFillMask @0x5ba750), per-region strip/poly mesh build (FindRegionOffset @0x5ba824), the 344-byte WaterMesh array (Floor+0x19E0/+0x1C6D). W5-WATER reconstructed the per-frame animation VIBE_Floor_AnimateWaterVertices @0x5be428 (16-vertex sine/cosine wobble + texture-set advance). Real AUGSBURG HAS water: 25 regions / 651 spans / 3712 polys built. **GAP (next wave): the water RENDER arm of @0x5bf22c (drawing the animated region meshes into the city frame) is not yet reconstructed/wired — BuildWaterRegions is exercised by its tests, ready for the render wave.** |

| WAVE-16 TRUE 1:1 BINARY DIFF — deep game systems (2026-06-15, 13-agent fleet, MCP LIVE) | progress/diff-*-wave16.md | Extended the wave-15 binary diff to the world/sim/play deep systems — decompile + line-for-line vs the reconstruction, fix divergences to the binary, resolve the wave-12/14 NEEDS-LIVE-MCP queues. Real 1:1 divergences FOUND + FIXED: **law** — the 26×36 Gesetz law table (unk_631E98) record-1 was 32 bytes (missing 4 zero bytes → drifted op/threshold/string fields); restored from get_bytes + 3 goldens corrected; plus StraftatBroadcast recipient-mask side effect, GesetzLoad clear-field +18, OfficeTallyCategoryCounts. **office** — promotion-cost matrix is a flat **stride-7 / 57-entry** array (the wave-12 OOB flag — not float[7][7]; recovered the real bytes); election winner seeded with incumbent wealth. **mission** — ~10 fixes incl. ClassifyLocationKind was FABRICATED (replaced with the real type codes from @0x51defc), MissionRequirementAdvance returns true for trackable-but-mismatched (+0x1C inc only on exact match; e2e golden corrected), reward field split, citizenship grant-cap loop; season `day%4` is **signed** (@0x58339c, NOT `&3`). **AI** — same npc_daily signed-season fix (removed the wave-12 `&3` + golden); 69-entry dispatch table confirmed. **char** — table extents 731/72/64 (not 256), CollectByOwner 1-based, RegisterHandlers catalog, PreloadSceneAnimations 18→16. **cut** — BuildingValue production-worth factor array width **2→6**, the inverted stock worth-loop mapping, auction ask-raise gate. **econ** — foreclosure creditor-marker gate; other 7 items 1:1. **hist** — HistoryTokenSlot token[4]→[5]; reconstructed the 3 deferred dialog bodies. **city** — CsvFieldList trailing token (the ObjByte/kindWorth OOBs confirmed 1:1). **combat** — ComputeDefenderStrength per-slot emission. **slice** — theatre participant column. **screens** — VERIFIED-1:1. The he.h misaligned-accessor + signed-season questions both RESOLVED as 1:1. Suite 1494/1494. |
| WAVE-16 DIFF-RENDER (2026-06-15, true 1:1 binary diff, IDA MCP live) | [diff-render-wave16.md](diff-render-wave16.md) + [diff-terrain-wave16.md](diff-terrain-wave16.md) | Line-for-line diff of the whole `src/render/` world-entity cluster (sky/sun/daycycle/fog/falloff, water, terrain, shadow, particle, mirror/sprite/node-lod/env-map/reflective, cloth/vegetation/.TXS) vs fresh decompiles; ConvertX-truncate audited across every float→int site. **Two big deferrals RESOLVED**: (1) the **x87 particle phase chain** — Fmod @0x5d3fb2 is `fprem`=st0 mod st1, Hex-Rays mistracked the deep-stack `pi` copy → bogus `Fmod(pi,pi)`; the truth is 5 INDEPENDENT accumulators `ang_i=fmod(dt·rate_i+ang_i,π)`; en route FIXED UpdatePoints velocity `dt·` not `phase·`, and UpdatePolys/UpdateLens use the 0x50../0xB4 rates not 0x80../0xB0; (2) the **terrain Pass-A slope buffer + LOD stitch** (see diff-terrain row — proxy removed via the real BuildTilePolys @0x5bc45c slope buffer; 4 stitch arms recovered, emission rule-8 deferred). FIXED divergences: BlendAmbientFog @0x5b8b04 colour form `(B−A)·frac+A` (truncate, not `a(1−t)+bt`); water texAccumB independent accumulator @0x5be428; Shadow_RasterizeTriangle @0x603ed4 (3 winding/apex bugs) + ProjectVertexPoint/RasterizeHeightField/BuildGroundShadow; PrepareReflectionNode @0x5F676C plane normal (was a cheap analogue → rotate stored normal, `d=n·v0`); QuadPolyVisible truth table + StampSlopeLight @0x5bc7b3 round-nearest; BuildVegetationCache @0x5c8560 quantize round-nearest (bare fistp). CONFIRMED: fog clamp = 255.0 (not 256.0); GetSeasonFromDay @0x58339c = signed `day%4`; vegetation = relight only (no sway); flags = skeletal .baf (no cloth-wave); .TXS magic 0x23F209AE set-major packed names. All owned render suites green, 0 failures. Line-for-line diff of the terrain cluster vs the FRESH full @0x5bf22c decompile (the prior "lost dump" gaps resolved). RESOLVED GAP 1: the per-quad diagonal split was a PROXY (`types[tc&mask]`); the real selector reads the per-LOD SLOPE buffer v379 = *(Floor+4*(lod>>1)+36) — the BuildTilePolys @0x5bc45c output (same +36 buffer, cell-stride dword_5B8CEC={1,2,4}; its SIGN drives the split, its 0x40 bit the sub-texture id) — now read via mipTexSrc[lod>>1] with the exact raw v374/v413 advance, proxy removed. RESOLVED GAP 2: the four LOD-boundary stitch arms (lines 848..1691) FULLY decompiled + vertex layout recovered (half-step v387=lod>>1 midpoint vert, 0.5 edge-UV blend, in-place far-vertex re-point + 1 appended poly stamped 16.0f, RIGHT/LEFT/UP/DOWN gates v487<7/v487>0/v486>0/v486<7) and documented; emission DEFERRED (rule 8) — it patches the engine's 80-byte 2-tri record via raw pointer surgery vs this repo's split 40-byte Polygons, with NO golden (fires only for unequal-LOD adjacency, in no captured scene) — gate-only kept, polyCount not inflated; the wave-4/10 DOWN/UP arm-gate swap fixed. FIXED: QuadPolyVisible decision tree (real nested cascade, Hidden fall-through; truth table [0,2,1,0,1,0,2,2,2,1,0,1,0,1,2,0], old golden was wrong); StampSlopeLight lit-value rounding (bare fistp @0x5bc7b3 = round-nearest via std::lrint, was trunc). VERIFIED-1:1: ComputeFilterWeights UV-table half (all 24 floats), BuildLitTileGeometry (3 modes), ComputeTileIllumination + 15-pattern table, ComputeLodLevel distance ladder (thrFar=Floor+7272, thrNear=Floor+7268, 40.0/0.4 bias, 8-frame debounce), SelectTileMeshLod (50.0/scaleX). ConvertX truncation audit across the cluster. Tests: render_terrain_walk 1629, render_tile_lighting 126, + 11 more cluster suites all 0 failures; consumers (city_view3d/wire_terrain_bridge/scene_floor/universe_e2e) green. |
| WAVE-6 WORLD-ENTITY 3D RENDER (2026-06-14, 12-agent fleet, IDA MCP live) | per-slice files below + [frame-integration-wave6.md](frame-integration-wave6.md) | Twelve world-entity render features reconstructed 1:1 from the decompile (software raster, presented via the existing Vulkan/SDL path per rules 3-4) and WIRED into the live CityView3D universe frame in BeginUniverseFrame @0x5b3900 order, each behind an additive default-OFF Options flag (existing frame pins stay byte-identical; the session opts all in). Full suite 1447/1447. The features: **water** ([water-render-wave6.md](water-render-wave6.md) + [water-regions-wave5.md](water-regions-wave5.md) — VIBE_Floor_TransformTileGeometry @0x5be668 water arm + BuildWaterRegions @0x5ba95c + AnimateWaterVertices @0x5be428; AUGSBURG 1 region/400 polys); **sky** ([sky-render-wave6.md](sky-render-wave6.md) — the time-of-day clear colour via BlendAmbientFog @0x5b8b04, pre-terrain); **drop shadows** ([shadow-render-wave6.md](shadow-render-wave6.md) — RenderMeshShadow @0x5f363c silhouette-flatten + RasterizeTriangle @0x603ed4, per caster ×4 lights); **particles** ([particle-render-wave6.md](particle-render-wave6.md) — RenderSystem @0x5e1278 billboard quads, blend/additive, after objects); **snow** ([snow-render-wave6.md](snow-render-wave6.md) — Snow_Render @0x42b5b0 + UpdateScene @0x42a2cc teardown, winter-gated); **rain+weather** ([rain-weather-wave6.md](rain-weather-wave6.md) — rain streaks @0x429c38 + Weather_UpdateSky @0x4c0040 state machine); **mirror/reflections** ([mirror-render-wave6.md](mirror-render-wave6.md) — BuildMirroredGeometry @0x5F637C reflect+clip, 5-term gate @0x5b3af0); **fog** ([fog-render-wave6.md](fog-render-wave6.md) — D3D vertex-fog reconstructed as per-pixel BlendFog565; corrected the 256.0→255.0 clamp misread); **sun/day-night** ([sun-daycycle-wave6.md](sun-daycycle-wave6.md) — ComputeSunState: season×time→sun vector + ambient/band curves, feeds sky/light/shadow); **world sprites/billboards** ([sprite-render-wave6.md](sprite-render-wave6.md) — UpdateBillboards @0x5AC970 project+depth-scale+depth-fade); **dynamic vertex lighting** ([vertex-lighting-wave6.md](vertex-lighting-wave6.md) — Light_ApplyToCachedVertices @0x5c6f90 point+sun, corrected per-vertex sun scale 0.01); **mesh/node LOD** ([node-lod-wave6.md](node-lod-wave6.md) — SelectLodFrame @0x5adb6c distance→LOD verified 1:1). Each slice ships golden + render tests; the fog/256.0, lighting/0.01, and snow/mis-named-function corrections were caught by re-decompiling against the live binary. |

| WAVE-7 RENDER REFINEMENTS (2026-06-14, 10-agent fleet + integration, IDA MCP live) | per-slice files + [frame-integration-wave7.md](frame-integration-wave7.md) | Closed the wave-6 rule-8 named gaps, each reconstructed 1:1 from the decompile inside its already-wired module: **shadow ground quad** ([shadow-ground-wave7.md](shadow-ground-wave7.md) — BuildGroundShadow @0x5f3048 + ProjectGroundQuad @0x5f216c + RasterizeHeightField @0x5f2a58, terrain-conforming soft shadow); **shadow node driver** ([shadow-node-wave7.md](shadow-node-wave7.md) — UpdateNodeShadows @0x5f4494 + CastFromLight @0x5f3f98 caster-slot mgmt); **particle integrators** ([particle-integrate-wave7.md](particle-integrate-wave7.md) — UpdatePoints @0x5e1e0c / UpdatePolys @0x5e2814 / UpdateLens @0x5e32c0 per-type physics); **mirror scene-graph** ([mirror-scenegraph-wave7.md](mirror-scenegraph-wave7.md) — PrepareReflectionNode @0x5f676c + CreateClippingPlanes @0x5f5d08 + CreateOutline @0x5F58FC); **per-pixel fog** ([fog-perpixel-wave7.md](fog-perpixel-wave7.md) — the vertex+79 factor carried as a 3rd interpolated 16.16 span channel, D3D linear vertex fog 1:1, replacing the per-triangle first cut); **bone-matrix vertex lighting** ([vertex-lighting-normals-wave7.md](vertex-lighting-normals-wave7.md) — the type-7 per-vertex NdotL with the real object-space normal + bone-world transform @0x5c7129; corrected wave-6's "shade-ramp empty" — flt_1405110 IS the angular falloff LUT written by InitFalloffTable @0x5c88f8 via flt_140510C+4); **water texture + poly linkage** ([water-texture-wave7.md](water-texture-wave7.md) — EF_WASS draw key ((tex-base)>>7)+1, scroll accumulators on the poly, FindRegionOffset @0x5ba824 per-tile point linkage); **sky band table** ([sky-bands-wave7.md](sky-bands-wave7.md) — the per-scene 6/7-band gradient loaded from Scene_LoadFromStream @0x5e7e38's light rig, driving BlendBandLighting @0x5b85e4 → BlendAmbientFog); **env-map reflections** ([envmap-wave7.md](envmap-wave7.md) — ComputeVertexLighting @0x5c9054 spherical reflection UVs for `texRec+104 & 1` reflective materials, verified 1:1); **billboard tint** ([sprite-tint-wave7.md](sprite-tint-wave7.md) — UpdateBillboards @0x5AC970 nodeType≥5 effect-tint arm, completing the function). Integration ([frame-integration-wave7.md](frame-integration-wave7.md)) wired sky-bands + EF_WASS water + per-pixel fog into the live frame (additive, session opts in; defaults byte-identical) — frame now 3561 distinct colours (wave-6: 2299). Shadow-driver/particle-integrator/mirror/bone-lighting/env-map are reconstructed but their per-instance data (object-space normals, reflective nodes, live particle systems) isn't carried by this view's .bgf instances — named rule-8 boundaries, not faked. Fidelity corrections caught by re-decompiling: fog clamp 255.0 not 256.0; per-vertex sun scale 0.01 not 0.001; the shade-ramp LUT writer; the snow/water function mis-namings. Suite 1452/1452. |

| WAVE-8 WORLD-ENTITY DATA + SYSTEMS (2026-06-14, 10-agent fleet, IDA MCP live) | per-slice files below + [frame-integration-wave8.md](frame-integration-wave8.md) | The data plumbing that makes the wave-6/7 render reconstructions fire, plus new world systems — all 1:1. **Mesh normals** ([mesh-normals-wave8.md](mesh-normals-wave8.md) — VIBE_Mesh_ComputeVertexNormals @0x5D1A6C static gen + the instance vertex+0x48 source-normal binding, so bone-matrix lighting + env-map have real normals); **scene lights** ([scene-lights-wave8.md](scene-lights-wave8.md) — Light_CollectAffectedObject @0x5c80a0 cull + BuildObjectCache @0x5c8218 collect, feeding LightMeshVertices; the engine's mixed-unit point-cull quirk preserved); **particle emitter creation** ([particle-emitter-wave8.md](particle-emitter-wave8.md) — CreateEmitter @0x43fd24 → SpawnSystemByType @0x5e3ae0 → AllocSystem @0x5e1000 tail-link into the live list dword_1408438; the `CreateEmitter` script command is the scene→emitter path); **reflective nodes** ([reflective-nodes-wave8.md](reflective-nodes-wave8.md) — the reflective marker is texture +104 bit5 set by LoadByName @0x5DA714 = high-shift material + real palette; the city's reflector is water via the floor path); **NPC clip selection** ([npc-clip-wave8.md](npc-clip-wave8.md) — Character_Update @0x405148 action-queue vs idle gate; gait "bewegung/gehen" when moving, "stehen/stehen_newnoise" idle); **2D overview map** ([mapview-wave8.md](mapview-wave8.md) — MapView_PanelDispatcher @0x5441d0 render + marker draw + click→world, reusing the marker projection @0x5440b4); **animals** ([creature-wave8.md](creature-wave8.md) — VERIFIED the RATTE/HUND/KATZE/PFERD subsystem (Animal_Update @0x48364c + spawn/herd/wander) was already reconstructed 1:1 in sim/animal*; only the live-tick wiring remained); **vegetation** ([vegetation-anim-wave8.md](vegetation-anim-wave8.md) — FINDING: the engine has NO wind sway; the per-frame veg update is the relight BuildVegetationCache @0x5c8560, reconstructed); **flags/cloth** ([cloth-anim-wave8.md](cloth-anim-wave8.md) — FINDING: no vertex cloth-wave; flags are skeletal .baf waved by the pose driver; reconstructed the attach plumbing AttachFlag @0x4b5d98/RefreshFlagAnimation @0x4b5ef8); **chimney smoke** ([building-fx-wave8.md](building-fx-wave8.md) — SpawnChimneySmoke @0x4b60a0 at the dummy_RAUCH_0 node via the Schornstein_dunkel.esc effect script + RefreshBuildingEffects @0x504910 driver, season/daylight-gated). Suite 1461/1461. Frame/session integration in [frame-integration-wave8.md](frame-integration-wave8.md). |

| WAVE-9 INTEGRATION COMPLETION (2026-06-14, MCP offline — wiring only) | [particle-runtime-wave9.md](particle-runtime-wave9.md) + [frame-enrich-wave9.md](frame-enrich-wave9.md) | With the IDA MCP disconnected (no new 1:1 reconstruction possible without it), this wave CONNECTED already-reconstructed pieces — no decompiling. **Particle runtime loop** completed: `ParticleSystemList::WalkAndUpdate` now runs the reconstructed per-type integrators (UpdatePoints/Polys/Lens) before render, and SpawnEmitterAtPosition rebuilds the genuine 0x310 emitter image so the integrator's slot offsets line up — live systems now produce + integrate + render moving/fading particles (the x87 turbulence-phase residual stays the documented wave-7 boundary, not faked). **CityView3D instance enrichment** fired the wave-8 deferred features behind additive default-off Options (session opts in): vegetation relight (BuildVegetationCache — 460 meshes/9676 verts/frame on AUGSBURG), animal render-arm (animals now draw through the character path — 6 drawn), flag attach/refresh (RefreshFlagAnimation per dummy_FAHNE; the .baf wave needs the building pose driver — documented), reflective-mesh scan (idle on AUGSBURG — its only reflector is water via the floor path), and the particle WalkAndUpdate call. Suite 1463/1463; defaults byte-identical (DefaultsByteIdentical e2e). Remaining session-condition items (not regressions): chimney smoke spawns 0 systems on the default AUGSBURG load (no reached dummy_RAUCH), NPC gait needs a moving person (default session has one idle person); both fire when their data conditions hold. The faithful x87 particle integrators + any further new reconstruction resume when the MCP reconnects. |

| WAVE-10 MEMORY-SAFETY HARDENING (2026-06-14, 10-agent fleet, MCP offline) | progress/harden-*-wave10.md | With the IDA MCP down (no new 1:1 reconstruction), a parallel ASAN+UBSAN + degenerate-edge pass over the ~40 modules from waves 6-9. Real bugs found + fixed FAITHFULLY (in-bounds path byte-identical; the original didn't corrupt memory either — behavioral ambiguities were flagged for MCP, not changed): **raster** — signed-shift + accumulator integer-overflow UB across the span/edge interpolators (WrapAddI32); **shadow** — OOB write on non-square shadow surfaces + over-large light-count OOB read + shift UB; **particle** — 4 leaks (AllocSystem allocations never freed) + null derefs + double-unlink guard; **water** — FloodFillMask native stack overflow (recursion→heap work-stack; output identical) + 3×3-dilation overflow UB; **terrain** — Pass-B LOD-stitch heap-buffer-overflow (count inflated past emitted polys) + falloff-LUT OOB clamp; **sky** — band-array OOB guards; **light** — point-light-list null deref; **mesh/sprite/lod** — normal-gen OOB + RLE shift-count UB (x86 `& 31`) + LOD null deref; **texture** — palettize null-ptr-offset on 0-area + shape_convert16 stack-array overrun on malformed shapes; **sim** — 3 season-index OOB (`& 3`) + animal cap-write OOB. Plus the orchestrator fixed the cross-cluster **compress::Inflater leak** (missing destructor → `b_`/codes leaked on every zip extract; added `~Inflater` = inflate_blocks_free @0x5ffa80) — real-asset ASAN now clean. ~150 new degenerate/edge tests added. Suite 1464/1464; both normal and ASAN+UBSAN builds green. BEHAVIORAL-needs-MCP items (unclamped tile-span core, clip 128-vtx envelope, terrain stitch vertex layout, a few div-by-zero matching the binary's own `idiv`) documented per-module, not changed. |

| WAVE-11 FOUNDATIONAL HARDENING (2026-06-14, 12-agent fleet, MCP offline) | progress/harden-*-wave11.md | ASAN+UBSAN + malformed/truncated/oversized-input pass over the FOUNDATIONAL parser/loader layers (io, compress, config, render asset loaders, gfx/scene/shape, gui, crt, sim command pipeline, sim entities, audio, net, app/shim_impl, render anim/camera). ~30 real bugs found + fixed FAITHFULLY (valid-asset path byte-identical; behavioral ambiguities flagged for MCP, not changed). Highlights: **io** — overlapping strcpy, dir-tree leaks, misaligned reads, save/zip truncation guards (7 bugs); **compress/config** — INI int-parse signed-overflow UB; **render loaders** — mesh_load matIndex OOB, anim_load count overflow, bgf/model_io heap-exhaustion-on-declared-count; **gfx/scene** — scene-node recursion stack overflow + untrusted `reserve` OOM + BMP degenerate-dims UB; **gui** — more form-table OOB writes (the wave-2 512-into-511 class repeated) + misaligned dword reads + a text-label leak; **crt** — printf `%s`/`%p` overflows, scanf float overflow, atoi/strtol overflow UB; **command pipeline** — 4 packet-parse OOB (field-patch cursor, fragment reassembly, flag-blob, param-refs); **entity** — scene-scan + CreateMesh strcpy + name-pick OOB; **audio** — handle-scan + path-rewrite overflows; **net** — ReceivePacket length-underflow OOB write; **app** — config_write INT_MIN negate UB; **anim** — misaligned bone-pointer reads. Plus the orchestrator fixed the cross-cluster items no agent owned: **world/data_load** — the byte-view provenance + a real OOB read of `g_kindWorth[25]` indexed by a record byte >23 on the real boot (bounded fail-safe + flagged for MCP @0x5833b4); **render/mesh_transform** misaligned pointer read (memcpy); a **wire_worldnet** test fixture 1-byte over-read. ~400 new malformed-input tests. Suite 1472/1472; normal + ASAN+UBSAN builds green. The earlier compress::Inflater leak (wave-10) confirmed gone on the real-asset path. BEHAVIORAL-needs-MCP items (the data_load subtype table size, several packet dest-bounds, blitter no-clip envelopes, div-by-zero matching the binary's `idiv`) documented per-module. |

| WAVE-12 WORLD/SIM/PLAY HARDENING (2026-06-14, 13-agent fleet, MCP offline) | progress/harden-*-wave12.md | ASAN+UBSAN + malformed/boundary pass over the remaining uncovered tree: world (economy/trade, history/stats, law/crime/trial, office/election/guild, mission/event/location, family/city/relation), sim (combat, AI/npcaction, character/charaction, cutscene/building-econ), play (menu/screens, slices/dialog), render (leaves/recon/misc). ~30 more real bugs found + fixed faithfully (valid path byte-identical). Highlights: **econ** — rate-scalar negative-index OOB, drag-slot column walk OOB; **history** — `memcmp`-over-read on NUL-less command tokens (→strncmp), chronicle index guards; **law** — gesetz save-load count OOB into the 512/4096 tables, evidence-collect buffer overflow; **office** — office-def table OOB on malformed holder type, guild-member collect overflow; **mission** — religion-cheat token over-read; **city** — groundplan grid OOB, save-io 32-bit count overflow bypass, census district-tile OOB, UtilParseInt overflow UB; **combat** — negative-velocity shift UB + packet-builder id+4 overflow; **AI/char** — candidate-buffer over-reads, the walk-on-path waypoint-at-255 over-read; **cutscene** — cheat-token over-read, per-type table index OOB, participant-list over-read, building-value factor-index OOB; **screens** — credits-crawl negative-Y heap overflow; **slices** — item-label NUL-less over-read, relation-snapshot OOB read. Plus the orchestrator fixed the cross-cluster items no agent owned: **io/save_browser** unbounded enumerate (>caller-capacity .SAV files → OOB) — additive `maxRecords` bound (default unbounded = 1:1; the two real callers now pass 256) + pin test; a **cutscene_misc5_test** stack-use-after-return (test stashed a pointer to a callee's local — now copies). ~300 new malformed-input tests. Suite 1485/1485; normal + ASAN+UBSAN green. BEHAVIORAL/cross-cutting items DOCUMENTED for MCP, not changed: the shared `sim/he.h` packed-record reference accessors are alignment-only UBSAN (benign on x86/x64, faithful to the GUILD_PACKED 1:1 layout — a fix would risk the serialization stride); `world/relation.cpp` Get/Set are unbounded like the binary (all callers now guard); `render/text_raster.cpp DrawGlyph` has no negative-coord guard (faithful — the original relied on form/window clipping; the credits caller was guarded). |

| WAVE-13 1:1 FIDELITY AUDIT of the playable flow (2026-06-14, 10-agent fleet, MCP offline) | progress/oneone-*-wave13.md | The MCP-free half of "1:1 comparison": every function on the live playable flow (boot/config → new-game commit → world/save load → render frame spine → raster leaves → clock/tick/turn → command apply/queue → camera → HUD/panels/input → persons/lighting) cross-checked against its in-tree evidence (provenance comments + progress docs), with its recovered 1:1 values PINNED by golden tests and a per-segment confidence map. RESULTS: **zero RED-FLAG functions** — every reconstruction on the flow carries a `// gilde.exe 0xXXXX` provenance address; **no source drift** found (one comment-address fix in game_day.h step-25: 0x5336e1→0x5320f0 to match the authoritative golden list). New golden pins added across all 10 segments for recovered values that were *used* but never *asserted*: the exact 45-key config-write order + cold defaults + boot constants (0x20007/0x24087); the opcode-11/27 builder wire layouts + Person_CreateAndSpawn record stamps (alive+8==100); the .cty/save record strides/biases/version-range + tag 0x3A6C00BB; the BeginUniverseFrame subsystem call order + 1e10 near-sentinel + the dual fps windows; the HiColTab dbl_6295E8=0.5 mid-ramp rounding; the 994ms tick + round-half-even advance + GameTime year-bias + the 26-step BeginPlayerRound order; the packet geometry constants (stride 153, ring 0x8000, ACK 10-byte layout) + the 486-byte producer table raw bytes; the camera inverted-axis pitch / ZoomOut listener-104 / OrientToTarget eye constants; the per-vertex sun scale 0.01 (bit-exact 0x3c23d70a) vs per-poly 0.001. Suite 1488/1488. **Each segment doc carries a NEEDS-LIVE-MCP list = the precise decompile targets for the TRUE binary diff when the IDA MCP reconnects** (e.g. the 0x533a54/0x52f2ec boot-step bodies, the non-partial .SAV tail @0x5a7604, the BeginUniverseFrame elided poly-counter resets, the clock proc 0x527778 x87-vs-double bit-identity, ZoomReset/ZoomOut numerics, the pose-driver bone/morph leaves). The reconstruction is now provenance-complete and golden-value-pinned end to end across the playable path; the live binary comparison is the remaining step, gated on MCP. |

| WAVE-15 TRUE 1:1 BINARY DIFF of the playable flow (2026-06-15, 10-agent fleet, MCP LIVE) | progress/diff-*-wave15.md | The MCP reconnected → the REAL binary diff. Each playable-flow segment's functions were decompiled and compared LINE-FOR-LINE against the reconstruction, fixing every divergence to match the binary and resolving the wave-13 NEEDS-LIVE-MCP queues. **Real 1:1 divergences FOUND and FIXED** (the in-tree audit could not catch these): (1) **VIBE_Coord_ConvertX @0x5c6b08 TRUNCATES toward zero** (sets x87 CW high byte 0x1F = RC=11, then frndint) — multiple modules had modeled it as round-to-nearest (`nearbyint`). Fixed to `trunc` at every confirmed call site: the game clock ScaledGameSeconds @0x527778 (off-ladder speeds), input_recon_select pick rounding, combat RoundTier @0x57eb64 (threat tiers), charaction FadeAlpha @0x40b998, rain streak colour @0x429c38 — each decompile-confirmed at its call site + golden corrected to the truncate values (the old goldens were wrong). (2) **ExApplyNeedDeltas @0x497ed0** clamped to 1000.0 but the binary stores **1024.0** on overflow (dbl threshold 1000.0, stored value 0x4090000000000000) — fixed. (3) **MoneyMultiplyByRate @0x58f19c** had the wrong formula entirely (was amount*rate/100; the binary is a city-currency table lookup `dword_649A88[dword_13CD6F2[189*id]>>16]`) — forwarded to the correct world:: reconstruction. (4) **BeginUniverseFrame/DrawUniverseAndStats** were missing per-frame counter resets, the ScrollUvCoords call, and the 64-list anim WalkAndInvoke loop — added. (5) **RelinkPersonRecordColumns @0x5abb84** had a spurious id==0 short-circuit + wrong He gate — fixed. (6) **GameInitWorldAndSounds @0x52f2ec** missing globals + the bit_cast<float>(1u) denormal param — fixed. VERIFIED-1:1 (no change): camera (whole segment), raster leaves (all — incl. the masked-span has NO runtime selector: the 24-bit colour-key is the DDBLT_KEYSRC-on-black upload path, confirming the wave-5 model), persons/lighting, new-game commit, frame clip/dispatch, the clock GameTime/Advance/BeginPlayerRound. Also resolved: the data_load kindWorth OOB is **verified 1:1** (the original over-reads into zero-BSS → returns 0, exactly the bounded fail-safe). Suite 1494/1494. Remaining: statchart/statpanel ConvertX idiom (off-flow, ShowCityStatistics not located by name — flagged NEEDS-MCP-VERIFY, left as shipped). The deep game-systems (world/sim non-flow) carry their wave-14 audit + NEEDS-LIVE-MCP queues for the next diff wave. |

| WAVE-17 CLOSE THE REMAINING DOCUMENTED GAPS 1:1 (2026-06-15, 4-agent fleet, MCP LIVE) | progress/gap-*-wave17.md | The deferred-gap closure pass. **Terrain LOD-stitch emission** ([gap-terrain-stitch-wave17.md](gap-terrain-stitch-wave17.md)) — reconstructed 1:1 the four stitch arms of VIBE_Floor_RenderTerrain @0x5bf22c lines 848..1691 (the 80-byte 2-tri record surgery → this repo's Polygons: midpoint vertex at the lod>>1 half-step, far-vertex re-point, appended poly stamped 16.0f, per-arm gates/advance), +41 synthetic unequal-LOD goldens; equal-LOD path byte-identical. Residual: the seam-UV midpoint-blend (flt_13FE540 UV table — that whole Pass-A UV-emission subsystem isn't modeled here; named with address). **Person_CreateAndSpawn @0x58da70** ([gap-person-create-wave17.md](gap-person-create-wave17.md)) — reconstructed the full no-parents path 1:1 (every field offset, the 14 stat triples, the talent/personality bytes, the guild/family slot counters) AND the exact ~1577 VIBE_Util_RandNext draw count/order so the new-game commit's downstream RandomModulo stream stays aligned; named hooks (rule 8) only for genuinely-missing-data leaves (family table word_13C3110, the runtime name tables dword_8C400C/4320/4508, avatar slot, head-bone) with their draws still made. **statpanel/statchart ConvertX** ([gap-statchart-wave17.md](gap-statchart-wave17.md)) — RESOLVED: located VIBE_StatPanel_ShowCityStatistics @0x55f4ac; ALL 14 chart/panel float→int sites TRUNCATE via ConvertX (the round-to-nearest model was wrong) — fixed + goldens corrected, incl. faithfully reconstructing the x87-80bit product precision (double-widening the series*height term) so it's bit-exact, not a boundary. **ProjectGroundQuad @0x5f216c** ([gap-cleanup-wave17.md](gap-cleanup-wave17.md)) — CLOSED: the "engine-private tile" is the live terrain scene record dword_64A028; reconstructed the projected-tile two-phase body 1:1 + golden. **He-record accessors** — CLOSED UBSAN-clean via `aligned(1)` typedefs in sim/he.h (the engine's native unaligned x86 reads, now alignment-sanitizer-clean) with ZERO consumer churn. The slider/favorability ConvertX sites verified as genuine runtime/preview boundaries. Suite 1495/1495. REMAINING genuine boundaries (documented, not gaps): the seam-UV blend (unmodeled UV subsystem), person-create missing-data leaves, the .esc/.sbf script VM (a whole subsystem, command/GUI-owned), and the RGBZ UV-scale x87-80bit-vs-SSE precision LSB (inherent to the SSE software port). |

| WAVE-18 RECONSTRUCT THE BOUNDARY SUBSYSTEMS (2026-06-15, 5-agent fleet + bg .esc, MCP LIVE) | progress/*-wave18.md | Closed the live-tree subsystems that previously left a gate hooked. **Family-record table** ([family-record-wave18.md](family-record-wave18.md)) — reconstructed word_13C3110 (16×82-word records) + Person_GetFamilyRecord @0x58c408 + the inline allocator @0x58ecf3 1:1; wired so the family gate in Person_CreateAndSpawn + ExCreatePersonB now FIRES (new src/sim/family_record). **Terrain UV-emission** ([terrain-uv-wave18.md](terrain-uv-wave18.md)) — reconstructed the flt_13FE540 per-quad UV table + the per-tile UV scratch + the seam-UV 0.5 midpoint blend (closing the wave-17 stitch boundary); new src/render/terrain_uvtable; equal-LOD path byte-identical. **Name tables** ([name-tables-wave18.md](name-tables-wave18.md)) — found the truth: dword_8C400C/8C4320/8C4508 are fixed-index slices of the TextDb dword_8C36B0, loaded from Text_C_Personen.res in textbin_deutsch.BIN; new src/sim/name_tables loads real localized names over the install + guarded e2e. **.esc effect-script VM** ([effect-script-wave18.md](effect-script-wave18.md)) — was already landed by a prior wave; became a verification pass that found+fixed two real control-flow bugs (inner-`}` prematurely popping the enclosing `while`; `++`/`--` neither lexed nor executed) + documented the keyword-subcode handoff. **Coverage audit** ([coverage-audit-wave18.md](coverage-audit-wave18.md)) — rule-7 BFS from the boot spine: ≈83.6% of 2140 entry-reachable functions reconstructed; of 351 missing, ~154 are genuine game logic (the rest DDraw/D3D/DInput→Vulkan, Miles→SDL, CRT/zlib runtime). Suite 1495/1495. |

| WAVE-19 CLOSE THE GAME-LOGIC GAP (coverage-driven, 10-agent fleet, MCP LIVE) | progress/*-wave19.md | Reconstructed the largest un-reconstructed logic clusters from the wave-18 audit, all reachable from the frame loop. **AI brain**: ai_needs ([ai-needs-wave19.md](ai-needs-wave19.md)) — AiNeeds_BuildScoreTable @0x4764e8 (60-method registrar) + the DFN loader (AI_DATA.DFN gzip, 61×73) + e2e; ai_meister ([ai-meister-wave19.md](ai-meister-wave19.md)) — CalcMeister Diebe/Farming/Wache/Ambush + CalcAngriff + EquipStaffWeapon + 16 sub-planners (also fixed a real wave-18 constant bug: RenovateRoomBudget 0.005/128.0/0.75). **Guild-office privileges**: privilege-a + privilege-b ([a](privilege-a-wave19.md)/[b](privilege-b-wave19.md)) — 21 privilege panels (Hatred/Blackmail/MakePeace/EnactLaw/Evidence/Embezzlement/…) + the ShowDialog dispatcher, float-truncation fidelity pinned (2% of 1050 = 20, not 21). **Building lifecycle** ([building-lifecycle-wave19.md](building-lifecycle-wave19.md)), **Groundplan window** ([groundplan-wave19.md](groundplan-wave19.md), real Riss_*.BMP e2e), **Object/Anim core** ([object-anim-wave19.md](object-anim-wave19.md)), **Character AI + terrain collision** ([character-ai-wave19.md](character-ai-wave19.md)), **Recruit/office/estate** ([recruit-office-wave19.md](recruit-office-wave19.md)), **Script-VM front-end** ([script-vm-wave19.md](script-vm-wave19.md)). CONSOLIDATION found+fixed a real 1:1 fidelity bug: the building agent's duplicate BuildingType_GroupFromCode @0x58a4c8 mis-modeled the 52-57 band as a ClassifyTypeFlag tail-call (→0) when the decompile shows JUMPOUT 0x58A27B (→10) and 58-63 as JUMPOUT 0x589850 (→2, a literal `mov al,2; ret`, NOT a tail-call) — removed the dup (ODR), kept building_type.cpp's correct version, fixed the test golden. Suite 1521/1521. |

| WAVE-20 FINISH THE ENTRY-REACHABLE LOGIC + WIRE WAVE-19 (coverage-driven, 13-agent fleet, MCP LIVE) | progress/*-wave20.md | Reconstructed the second-tier missing logic + wired the wave-18/19 handoffs into the live tree. **Reconstructed 1:1**: savestate/VfsWriteBuffered ([saveload](saveload-wave20.md), found "DecompressGameState" is actually the per-frame scene-update pass, not a codec), the gilde_text.def + label-definition parser ([text-def](text-def-wave20.md), real-asset e2e), drag-select/drag-cursor/edge-scroll/map-scroll ([dragselect](dragselect-wave20.md)), He entity messaging + icon-circle ([he-messaging](he-messaging-wave20.md)), the 4 statpanel graph-series plotters ([statpanel-graph](statpanel-graph-wave20.md), x87-double-widened, ConvertX-truncate), object transform/market-value ops ([object-ops](object-ops-wave20.md)), scene/building-interior entry + vegetation + combat-mesh ([scene-interior](scene-interior-wave20.md)), sky-dome/heightmap/floor-reload/camera-target ([skycam](skycam-wave20.md), self-caught a 64-bit heap overflow), chat/trade/item-use/hotkey/save-name ([uipanels](uipanels-wave20.md)), command/economy/turn-tick/config leaves ([cmdecon](cmdecon-wave20.md)), window scroll-content layout + RNG seed accessors ([window-scroll](window-scroll-wave20.md)). **Integration** ([integration-wave20.md](integration-wave20.md)) — WIRED 7 handoffs (name tables→person_create, script-command registration, Meister-AI dispatch, character vis/standup hooks, estate-transfer opcode 0x0D, building lifecycle/frame hooks, privilege panels-A) and found another real 1:1 bug (dynasty-name dst is rec+0x40, not +0x20). **Coverage refresh** ([coverage-audit-wave20.md](coverage-audit-wave20.md)) — **≈89.0% of 2152 entry-reachable functions reconstructed** (up from 83.6%), pure-logic debt 154→62 (125 of the original 154 closed). CONSOLIDATION fixed one wrong test include path (skycam). Suite 1536/1536. Remaining top logic: Entity_InteractionLogic/EntityChild_Process/Object_Update/Animation_Apply (the per-frame entity core, render-context-coupled), Anim_CreateMorphAnim, Map_ComputeRoadNetworkLayout, Rain/Snow update, the privilege-b/evidence sub-cluster, Recruit modal windows. |

| WAVE-21 THE PER-FRAME ENTITY CORE + REMAINING LOGIC, WIRED (coverage-driven, 8-agent fleet, MCP LIVE) | progress/*-wave21.md | Reconstructed the simulation heart + the rest of the high-value logic. **Per-frame entity core** ([entity-core-wave21.md](entity-core-wave21.md)) — the FULL bodies of Object_Update @0x40eea0 / Animation_Apply @0x415b78 / EntityChild_Process @0x418f34 / Entity_InteractionLogic @0x41078c, the real frame SCENE-UPDATE walk DecompressGameState @0x41ceb4 (NOT a codec — it walks the entity table dispatching the per-record updates) + Decompressor_Init @0x40e50c, and the per-record dispatcher Interactions @0x4139a8 (types 0x40-0x47 + Gfx_CrossFadeStep @0x41e814) — WIRED live into RunFrameLoop; e2e goldens unshifted. **Weather** ([weather-wave21.md](weather-wave21.md)) — Anim_CreateMorphAnim @0x5cf150 (morph-delta byte quantization, trunc) new; Rain_UpdateDrop verified (fixed a real bug: head-projection numerator uses flt_611674=0.5 not the dt-scale); Snow_UpdateFlake verified. **Road network** ([road-network-wave21.md](road-network-wave21.md)) — Map_ComputeRoadNetworkLayout @0x592d98 (actually the upgrade-tree Sugiyama DAG layout) + the 44-byte node store with field aliasing reproduced byte-exactly. **Ai_CalcBankmeister** @0x459264 ([ai-bank-wave21.md](ai-bank-wave21.md)) — bank-meister brain (interest-rate state machine + 3-tier vault mint/melt). **Privilege-b COMPLETE+WIRED** ([privilege-b-complete-wave21.md](privilege-b-complete-wave21.md)) — wave-19 shipped only predicate helpers; added the 10 full panel bodies + the SET-B dispatcher + InstallPrivilegePanelsB chained onto the shared hook, reachable end-to-end via contextaction2. **Recruit modals COMPLETE** ([recruit-complete-wave21.md](recruit-complete-wave21.md)) — RunRecruitmentOfferWindow/CandidatePick/HireConfirm full 1:1 bodies, internal chain wired. **Integration2** ([integration2-wave21.md](integration2-wave21.md)) — wired AI-data-DFN→InitGuardState along the real 0x4520d0 edge + Floor_ReloadTextures→SetGammaTable; corrected a wrong coverage claim (NpcAction_NotifyJoinLeaveGroup is NOT yet reconstructed). **Coverage** ([coverage-audit-wave21.md](coverage-audit-wave21.md)) — **≈90.5% of 2164 reachable reconstructed** (range-matched), logic debt 62→33. Suite 1542/1542. Remaining: the privilege evidence sub-cluster (EvidenceDetails/ReviewAlt/BuildEvidenceEntry), Widget_LayoutBounds, Amt_ComputeOfficeWages, TradePanel refresh, ChatConsole_BuildWindow, NpcAction_NotifyJoinLeaveGroup + Building_OpenUpgradeTreeWindow (unwired). |

| WAVE-22 CLOSE THE LOGIC TAIL + HARDEN UI 1:1 (coverage-driven, 9-agent fleet, MCP LIVE) | progress/*-wave22.md | Drove the logic tail toward the tech-boundary floor + a UI fidelity/safety hardening pass. **Privilege evidence** ([privilege-evidence-wave22.md](privilege-evidence-wave22.md)) — PanelEvidenceDetails @0x565b88 + BuildEvidenceEntry @0x56589c full bodies + EvidenceReviewAlt office-arm completed, WIRED into the SET-B dispatcher. **Chat/Trade** ([chattrade-wave22.md](chattrade-wave22.md)) — found wave-20's claims were sibling-address artifacts; reconstructed the REAL ChatConsole_BuildWindow @0x4bfc48 + the two-pass TradePanel_RefreshSell/ItemColumns @0x50bf08/0x50b1c4 (column tables get_bytes-pinned). **GUI core** ([guicore-wave22.md](guicore-wave22.md)) — Widget_LayoutBounds @0x413220, Gui_ResolveObjectState/MarkObjectUsed, Property_Get, State_Update/GetCurrent (the bodies render_submit called as hooks). **Office/law** ([officelaw-wave22.md](officelaw-wave22.md)) — Amt_ComputeOfficeWages @0x57b480 (ConvertX-trunc: 7×100×32×0.33f→7392), Gesetz_ComputeMaxWantedLevel, Person_QueryByGoodType. **Cmd-queue** ([cmdqueue-wave22.md](cmdqueue-wave22.md)) — verified the 6 request builders were already 1:1 in command_builders2 (added provenance + first test). **Small leaves** ([smallleaves-wave22.md](smallleaves-wave22.md)) — MapView_AddCornerObjects, DragSlot_BeginDragText, Input_SetIconTextById, Cutscene Get/SetRandSeed, Resource_FlushAndFree, Coord_Transform (found Coord_Transform is a struct-field add, not a projection). **Npc/Building** ([npcbuilding-wave22.md](npcbuilding-wave22.md)) — NpcAction_NotifyJoinLeaveGroup @0x4c9dec + Building_OpenUpgradeTreeWindow @0x594100 reconstructed+wired (closing the road_network handoff). **UI HARDEN** ([uiharden-wave22.md](uiharden-wave22.md)) — swept the mature gui modules for the ConvertX bug class + ran them under ASAN/UBSAN; fixed two real 1:1 divergences (gui_dialogs5 ChooseProfession dropped the ×0.015 wealth scale → now trunc(1000×0.015f)=14; gui_dialogs6 PlantBar missing the ×0.5+re-truncate → trunc(trunc(price)×0.5) floored 1024), 0 sanitizer issues. **Coverage** ([coverage-audit-wave22.md](coverage-audit-wave22.md)) — **≈90.85% of 2164 reachable reconstructed**, logic debt 33→25; corrected a wave-21 over-count (Rain_UpdateDrop @0x4294d4 was NOT actually closed — its cites belong to sibling Rain_GrowDropList) and reclassified 2 logic→CRT. Suite 1549/1549. The 198 missing now = 25 pure logic + 121 CRT/zlib/MBCS + 33 Miles + 11 DDraw/D3D + 5 DInput + 3 thunks (the last 173 are the genuine tech-boundary floor, NOT to reconstruct). |

| WAVE-23 THE LOGIC FLOOR (focused 3-agent fleet, MCP LIVE) | progress/*-wave23.md | Closed the last two genuine logic gaps + established the definitive count. **Rain_UpdateDrop** @0x4294d4 ([rain-updatedrop-wave23.md](rain-updatedrop-wave23.md)) — the wave-21 body was a snow-sibling guess (the wave-22 audit caught the over-count); reconstructed the real per-drop integrator 1:1 from the disasm, fixing 4 bugs (drift-matrix column grouping, velocity-X wind-vector transform, Z integration drift2×2.0 with off.z a dead store, camera-motion-compensation prevAnchor/prevEye snapshots) + extending RainSystem with the binary's +0x1C/+0x20/+0x40/+0x50 fields; no float→int in the integrator (only the colour pack truncates). **BuildEvidenceEntry** @0x56589c ([evidence-scan-wave23.md](evidence-scan-wave23.md)) — completed the full body wave-22 had deferred: the judge/witness ENTITY SCAN (the "boundary" was not genuine — reused the live person arrays + ObjectSearchFindOneByPaletteRange + QueueRequestSlotReset28), the 248-byte opcode-28 court record, judge/accuser sourcing, two palette-range witness searches; wired all three Evidence panels to the real full body (disasm-over-decompile: accuser stride 536, not Hex-Rays' 268). **Reconcile + final audit** ([coverage-audit-wave23.md](coverage-audit-wave23.md)) — added the missing TradePanel provenance cites; **FINAL: ≈91.91% of 2164 reachable reconstructed (1989/2164)**. THE LOGIC FLOOR IS REACHED: of the 175 still-unreconstructed, **0 are genuine game logic** — all are pre-approved tech boundaries (CRT/zlib/MBCS host runtime ~122, Miles→SDL 28, DDraw/D3D→Vulkan 16, DirectInput→SDL 5, thunks 2). Suite 1550/1550. Remaining documented loose ends (rule 13, NOT logic gaps): two reconstructed UI windows still unwired — NpcAction_NotifyJoinLeaveGroup @0x4c9dec (hook is 3-arg vs the real 4-arg; binding changes live behavior, needs golden verification) and Building_OpenUpgradeTreeWindow @0x594100 (blocked on its un-reconstructed caller OpenUpgradeWindow @0x50f7c0). |

## Conventions for these files
- State each function by **original address + symbol** when claiming it done.
- List what is **deferred** and **why** (rule 8: no fakes — name the gap).
- Record **test names + check counts** (rule 11).
- Note where each piece is **wired** into the live call tree (rule 13).
